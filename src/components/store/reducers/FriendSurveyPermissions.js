const initState = {
    friendsProfile: [
       {fname: 'test', lname: 'test', friendStatus: 'test'},
    ]
}

const FriendSurveyPermissionsReducer = (state = initState, action) => {
    return state
}

export default FriendSurveyPermissionsReducer